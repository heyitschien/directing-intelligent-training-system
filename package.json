{
  "name": "directing-intelligent-training-system",
  "private": true,
  "description": "Monorepo: Next.js Strategist OS (web) + Expo mobile app",
  "scripts": {
    "lint": "npm run lint --prefix web",
    "typecheck": "npm run typecheck --prefix web && npm run typecheck --prefix mobile",
    "build": "npm run build --prefix web",
    "validate": "npm run lint --prefix web && npm run typecheck --prefix web && npm run build --prefix web && npm run typecheck --prefix mobile"
  }
}
