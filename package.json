{
  "name": "directing-intelligent-training-system",
  "private": true,
  "description": "Strategist OS — Next.js workshop app (responsive web)",
  "scripts": {
    "lint": "npm run lint --prefix web",
    "typecheck": "npm run typecheck --prefix web",
    "build": "npm run build --prefix web",
    "validate": "npm run lint --prefix web && npm run typecheck --prefix web && npm run build --prefix web"
  }
}
